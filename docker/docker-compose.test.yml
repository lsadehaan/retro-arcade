services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ..:/app
      - /app/node_modules
    environment:
      - NODE_ENV=test
      - "DB_PATH=:memory:"
      - JWT_SECRET=test-secret-for-ci
      - CORS_ORIGIN=http://localhost:3000
